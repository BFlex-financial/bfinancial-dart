name: bfinancial
description: Library to assist with large and small scale payments
version: 0.0.1
repository: https://github.com/BFlex-Financial/bfinancial-dart

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  http: ^1.1.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
